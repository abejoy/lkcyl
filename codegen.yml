overwrite: true
schema: "./lib/graphql/schema.graphql"  # Adjust the path to your GraphQL schema
generates:
  ./lib/lambdas/helpers/types/graphql-types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-resolvers"
