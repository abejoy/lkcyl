type Player {
    name: String!
    verified: Boolean!
}

enum Color {
    Red,
    Green,
    Blue,
    White,
    Black
}

enum Gender {
    Male,
    Female
}

type AvailableColor {
    colorName: Color!
    available: Boolean!
}

type Team {
  teamName: String!
  managerName: String!
  managerEmail: String!
  captianName: String!
  captianEmail: String!
  teamColor: Color!
  kcylUnit: String!
  gender: Gender!
  additionalMessage: String
  players: [Player]
}

type Query {
  getTeam(teamName: String!): Team
  getAvailableColors: [AvailableColor]!
  getAllTeam: [Team]!
  getTableData: [[String]]!
}

type Mutation {
  addTeam(
    teamName: String!, 
    managerName: String,
    managerEmail: String!,
    captianName: String!,
    captianEmail: String!,
    teamColor: Color!,
    gender: Gender!,
    kcylUnit: String!
    additionalMessage: String
    playerNames: [String]!
  ): Team

}